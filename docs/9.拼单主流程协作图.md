```mermaid
sequenceDiagram
    participant 发起人
    participant 参与者
    participant 前端页面
    participant OrderController
    participant OrderService
    participant GroupOrder
    participant GroupOrderMember
    participant OrderReceipt
    participant CapitalRecord
    participant NotificationService
    participant OrderScheduler

    %% 第一阶段：发起人创建拼单
    发起人->>前端页面: 1. 提交拼单信息（商品名称、总人数、预估总金额、取货点、截止时间）
    前端页面->>OrderController: 1.1 createOrder(orderForm)
    OrderController->>OrderService: 1.2 createOrder(orderForm, currentUser)
    OrderService->>GroupOrder: 1.3 创建拼单对象（状态：拼单中）
    OrderService->>GroupOrderMember: 1.4 创建发起人成员记录（状态：待支付）

    %% 第二阶段：发起人完成支付
    发起人->>前端页面: 2. 发起支付（预估人均金额）
    前端页面->>OrderController: 2.1 payOrder(orderId)
    OrderController->>OrderService: 2.2 payOrder(orderId, currentUser)
    OrderService->>GroupOrderMember: 2.3 更新发起人支付状态（已支付）
    OrderService->>CapitalRecord: 2.4 记录支付流水（类型：支付）

    %% 第三阶段：参与者加入并支付
    参与者->>前端页面: 3. 查看拼单详情并点击加入，填写备注
    前端页面->>OrderController: 3.1 joinOrder(orderId, remark)
    OrderController->>OrderService: 3.2 joinOrder(orderId, remark, currentUser)
    OrderService->>GroupOrderMember: 3.3 创建参与者成员记录（状态：待支付）
    参与者->>前端页面: 3.4 发起支付（预估人均金额）
    前端页面->>OrderController: 3.5 payOrder(orderId)
    OrderController->>OrderService: 3.6 payOrder(orderId, currentUser)
    OrderService->>GroupOrderMember: 3.7 更新参与者支付状态（已支付）
    OrderService->>CapitalRecord: 3.8 记录支付流水（类型：支付）

    %% 第四阶段：系统自动成团
    OrderService->>GroupOrder: 4.1 检查人数是否满员（当前成员数 >= 总人数）
    OrderService->>GroupOrder: 4.2 检查是否全员已支付
    OrderService->>GroupOrder: 4.3 更新状态为 已成团
    OrderService->>NotificationService: 4.4 发送成团通知
    NotificationService-->>发起人: 成团通知
    NotificationService-->>参与者: 成团通知

    %% 第五阶段：上传订单凭证
    发起人->>前端页面: 5. 上传订单截图并填写实际金额、预计送达时间区间
    前端页面->>OrderController: 5.1 uploadReceipt(orderId, receiptForm)
    OrderController->>OrderService: 5.2 uploadReceipt(orderId, receiptForm, currentUser)
    OrderService->>OrderReceipt: 5.3 保存凭证记录（实际总金额、预计送达时间区间）
    OrderService->>GroupOrder: 5.4 更新实际总金额、预计送达时间区间
    OrderService->>GroupOrder: 5.5 更新状态为 待送达

    %% 第六阶段：差额退款
    OrderService->>OrderService: 6.1 比较实际总金额与预估总金额
    alt 实际金额 < 预估金额
        OrderService->>CapitalRecord: 6.2 为所有成员生成差额退款流水（实际人均 - 预估人均）
        OrderService->>GroupOrderMember: 6.3 累计退款金额
        OrderService->>NotificationService: 6.4 发送差额退款通知
        NotificationService-->>发起人: 差额退款通知
        NotificationService-->>参与者: 差额退款通知
    else 实际金额 >= 预估金额
        Note over OrderService: 超出部分由发起人自行承担或线下补收，平台不介入
    end

    %% 第七阶段：确认送达
    发起人->>前端页面: 7. 点击"商品已送达"
    前端页面->>OrderController: 7.1 markDelivered(orderId)
    OrderController->>OrderService: 7.2 markDelivered(orderId, currentUser)
    OrderService->>GroupOrder: 7.3 更新状态为 待收货
    OrderService->>GroupOrderMember: 7.4 将所有成员收货状态更新为 待收货
    OrderService->>NotificationService: 7.5 发送可取货通知
    NotificationService-->>参与者: 通知取货

    %% 第八阶段：参与者确认收货
    参与者->>前端页面: 8. 点击确认收货
    前端页面->>OrderController: 8.1 confirmReceive(orderId)
    OrderController->>OrderService: 8.2 confirmReceive(orderId, currentUser)
    OrderService->>GroupOrderMember: 8.3 更新成员收货状态为 已收货
    OrderService->>NotificationService: 8.4 发送收货确认通知
    NotificationService-->>发起人: 参与者收货确认通知

    %% 第九阶段：自动确认收货
    OrderScheduler->>OrderService: 9.1 定时检查待确认成员（发起人确认送达后30分钟）
    OrderService->>GroupOrderMember: 9.2 将超时成员收货状态更新为 已收货（自动确认）
    OrderService->>NotificationService: 9.3 发送自动确认收货通知
    NotificationService-->>参与者: 自动确认收货通知
    Note over OrderService,OrderScheduler: 发起人确认送达后，参与者若 30 分钟内未点击确认收货，系统自动确认收货

    %% 第十阶段：完成拼单与结算
    OrderService->>GroupOrder: 10.1 检查是否所有成员均已收货
    OrderService->>GroupOrder: 10.2 更新状态为 已完成
    OrderService->>CapitalRecord: 10.3 生成结算给发起人的流水（类型：结算，金额：实际总金额）
    OrderService->>NotificationService: 10.4 发送拼单完成通知
    NotificationService-->>发起人: 拼单完成通知（平台已打款）
    NotificationService-->>参与者: 拼单完成通知
```

**图 X-X 拼单主流程协作图**

**图注：**
本图展示拼单主流程中参与者、前端页面、控制类、服务类和核心实体之间的通信关系与职责分工。主要流程包括：发起人创建拼单、参与者加入并支付、系统自动成团、发起人上传凭证、系统执行差额退款、发起人确认送达、参与者确认收货、系统自动确认收货、以及完成拼单与结算等关键环节。协作图设计严格遵循需求文档 v6.2 的业务规则和状态定义。

**关键业务规则说明：**
1. **发起人发布拼单**：系统自动将发起人加入成员列表，发起人需完成支付（预估人均金额）
2. **参与者加入**：填写备注并完成支付，支付成功后占用拼单名额
3. **自动成团条件**：人数满员（当前成员数 >= 总人数）且所有参与者均已完成支付
4. **凭证上传要求**：成团后30分钟内必须上传订单截图、填写实际总金额和预计送达时间区间
5. **差额退款规则**：若实际金额 < 预估金额，系统自动退差额（预估人均 - 实际人均）；若实际金额 >= 预估金额，超出部分由发起人自行承担
6. **确认送达触发**：商品实际送达取货点后，发起人点击"商品已送达"，系统通知所有参与者可取货，并开启30分钟确认收货倒计时
7. **确认收货方式**：参与者主动确认收货或系统30分钟自动确认
8. **拼单完成条件**：所有参与者均已确认收货（含系统自动确认）
9. **平台结算**：所有参与者确认收货后，系统自动将实际总金额打款给发起人
10. **状态定义**：拼单状态包括 拼单中、已成团、待送达、待收货、已完成、已取消

**对象职责分工：**
- **发起人**：填写拼单信息、支付、上传凭证、确认送达
- **参与者**：查看拼单、填写备注加入、支付、确认收货
- **前端页面**：负责交互和请求发起
- **OrderController**：负责接口入口和请求转发
- **OrderService**：负责主业务流程控制和状态管理
- **GroupOrder**：负责拼单主状态承载和业务规则验证
- **GroupOrderMember**：负责成员状态承载（加入、支付、收货状态）
- **OrderReceipt**：负责凭证信息存储
- **CapitalRecord**：负责资金流水记录（支付、退款、结算）
- **NotificationService**：负责通知发送（成团、退款、送达、完成）
- **OrderScheduler**：负责自动化任务执行（超时检测、自动确认收货）
