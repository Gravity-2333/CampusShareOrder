```mermaid
classDiagram
    class UserAccount {
        -Long id
        -String phone
        -String passwordHash
        -String nickname
        -String studentNo
        -Boolean isVerified
        -Integer creditScore
        -String status
        -String contactInfo
        +verifyStudent()
        +updateProfile()
        +changeCreditScore()
    }

    class AdminAccount {
        -Long id
        -String username
        -String passwordHash
        -String status
        -Date createdAt
        -Date lastLoginAt
    }

    class GroupOrder {
        -Long id
        -String orderNo
        -Long creatorUserId
        -String productName
        -String productDesc
        -Integer totalMemberCount
        -Integer currentMemberCount
        -BigDecimal estimatedTotalAmount
        -BigDecimal estimatedPerAmount
        -BigDecimal actualTotalAmount
        -BigDecimal actualPerAmount
        -String pickupPoint
        -Date deadlineAt
        -Date receiptUploadDeadlineAt
        -Date expectedDeliveryStartAt
        -Date expectedDeliveryEndAt
        -Date deliveredAt
        -String status
        -Boolean complaintOpened
        -String cancelReason
        +createOrder()
        +canJoin()
        +canExit()
        +markGrouped()
        +uploadReceipt()
        +markDelivered()
        +markCompleted()
        +cancelOrder()
    }

    class GroupOrderMember {
        -Long id
        -Long groupOrderId
        -Long userId
        -Boolean isCreator
        -String remark
        -String joinStatus
        -String payStatus
        -BigDecimal payAmount
        -Date paidAt
        -BigDecimal refundAmountTotal
        -String receiveStatus
        -Date receivedAt
        +markPaid()
        +markExit()
        +markCanceled()
        +markWaitConfirm()
        +markReceived()
        +markAutoReceived()
    }

    class OrderReceipt {
        -Long id
        -Long groupOrderId
        -Long uploaderUserId
        -String imageUrl
        -BigDecimal actualTotalAmount
        -Date expectedDeliveryStartAt
        -Date expectedDeliveryEndAt
        -Date uploadedAt
        +saveReceipt()
    }

    class Complaint {
        -Long id
        -String complaintNo
        -Long groupOrderId
        -Long complainantUserId
        -Long accusedUserId
        -String type
        -String content
        -String status
        -String handleResult
        -Long handledByAdminId
        -Date handledAt
        +submitComplaint()
        +markProcessed()
    }

    class CapitalRecord {
        -Long id
        -String bizNo
        -Long userId
        -Long groupOrderId
        -Long memberId
        -String type
        -BigDecimal amount
        -String status
        -String remark
        +recordPay()
        +recordRefund()
        +recordSettle()
    }

    class CreditChangeRecord {
        -Long id
        -Long userId
        -Integer changeValue
        -String reasonType
        -Long relatedOrderId
        -Long relatedComplaintId
        -String remark
        +recordIncrease()
        +recordDecrease()
    }

    class Notification {
        -Long id
        -Long userId
        -String type
        -String title
        -String content
        -Boolean isRead
        -Long relatedOrderId
        -Long relatedComplaintId
        -Date readAt
        +sendNotification()
        +markRead()
    }

    class OperationLog {
        -Long id
        -String operatorType
        -Long operatorId
        -String bizType
        -Long bizId
        -String action
        -String detailJson
        +recordAction()
    }

    %% 关系定义
    UserAccount "1" --> "*" GroupOrder : 创建发起
    UserAccount "1" --> "*" GroupOrderMember : 参与
    GroupOrder "1" --> "*" GroupOrderMember : 拥有成员
    GroupOrder "1" --> "0..1" OrderReceipt : 上传凭证
    GroupOrder "1" --> "*" Complaint : 关联投诉
    UserAccount "1" --> "*" Complaint : 投诉人
    UserAccount "1" --> "*" Complaint : 被投诉人
    UserAccount "1" --> "*" CreditChangeRecord : 信用分变化
    GroupOrder "1" --> "*" CreditChangeRecord : 关联
    Complaint "1" --> "*" CreditChangeRecord : 触发
    GroupOrder "1" --> "*" CapitalRecord : 产生流水
    UserAccount "1" --> "*" CapitalRecord : 关联
    GroupOrderMember "1" --> "*" CapitalRecord : 关联
    UserAccount "1" --> "*" Notification : 接收通知
    GroupOrder "1" --> "*" Notification : 关联
    Complaint "1" --> "*" Notification : 关联
    AdminAccount "1" --> "*" Complaint : 处理
```

**图 X-X 校园拼单平台核心实体类图**

**图注：**
本图展示校园拼单平台的核心实体类及其静态关系。系统以 GroupOrder 和 GroupOrderMember 为中心，通过 UserAccount、OrderReceipt、Complaint、CapitalRecord、CreditChangeRecord、Notification 和 OperationLog 等实体共同支撑拼单主流程、异常投诉处理和后台审计功能。实体类设计与需求文档 v6.2 保持一致，涵盖用户管理、拼单全流程、伪支付伪退款、信用分体系和投诉处理等核心业务。

**核心业务规则说明：**
1. **信用分规则**：初始80分，完成有效拼单+2分，投诉成立-10分，主动放鸽子-15分，发起人未上传凭证-10分
2. **拼单状态**：拼单中、已成团、待送达、待收货、已完成、已取消
3. **用户-拼单状态**：待支付、已支付、待收货、已收货、已退款
4. **投诉类型**：仅支持"发起人未购买商品"和"发起人上传虚假凭证"两类
5. **投诉状态**：待处理、已处理
6. **支付机制**：平台代收代付，采用伪支付伪退款，不涉及真实资金流转
7. **交付机制**：发起人上传凭证后填写实际总金额和预计送达时间区间，确认送达后参与者30分钟内需确认收货，超时自动确认
