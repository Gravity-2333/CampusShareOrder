```mermaid
sequenceDiagram
    participant 发起人
    participant 参与者
    participant 前端页面
    participant OrderController
    participant OrderService
    participant GroupOrder
    participant GroupOrderMember
    participant OrderReceipt
    participant CapitalRecord
    participant NotificationService
    participant OrderScheduler

    %% 第一阶段：发起人创建拼单
    发起人->>前端页面: 1. 提交拼单信息
    前端页面->>OrderController: 1.1 createOrder(orderForm)
    OrderController->>OrderService: 1.2 createOrder(orderForm, currentUser)
    OrderService->>GroupOrder: 1.3 创建拼单对象
    OrderService->>GroupOrderMember: 1.4 创建发起人成员记录

    %% 第二阶段：发起人完成支付
    发起人->>前端页面: 2. 发起支付
    前端页面->>OrderController: 2.1 payOrder(orderId)
    OrderController->>OrderService: 2.2 payOrder(orderId, currentUser)
    OrderService->>GroupOrderMember: 2.3 更新发起人支付状态
    OrderService->>CapitalRecord: 2.4 记录支付流水

    %% 第三阶段：参与者加入并支付
    参与者->>前端页面: 3. 查看拼单详情并点击加入
    前端页面->>OrderController: 3.1 joinOrder(orderId, remark)
    OrderController->>OrderService: 3.2 joinOrder(orderId, remark, currentUser)
    OrderService->>GroupOrderMember: 3.3 创建参与者成员记录
    参与者->>前端页面: 3.4 发起支付
    前端页面->>OrderController: 3.5 payOrder(orderId)
    OrderController->>OrderService: 3.6 payOrder(orderId, currentUser)
    OrderService->>GroupOrderMember: 3.7 更新参与者支付状态
    OrderService->>CapitalRecord: 3.8 记录支付流水

    %% 第四阶段：系统自动成团
    OrderService->>GroupOrder: 4.1 检查人数是否满员
    OrderService->>GroupOrder: 4.2 检查是否全员已支付
    OrderService->>GroupOrder: 4.3 更新状态为 GROUPED
    OrderService->>NotificationService: 4.4 发送成团通知
    NotificationService-->>发起人: 成团通知
    NotificationService-->>参与者: 成团通知

    %% 第五阶段：上传订单凭证
    发起人->>前端页面: 5. 上传订单截图并填写实际金额、预计送达时间区间
    前端页面->>OrderController: 5.1 uploadReceipt(orderId, receiptForm)
    OrderController->>OrderService: 5.2 uploadReceipt(orderId, receiptForm, currentUser)
    OrderService->>OrderReceipt: 5.3 保存凭证记录
    OrderService->>GroupOrder: 5.4 更新实际总金额、预计送达时间区间
    OrderService->>GroupOrder: 5.5 更新状态为 WAIT_DELIVERY

    %% 第六阶段：差额退款
    OrderService->>OrderService: 6.1 比较实际总金额与预估总金额
    alt 实际金额 < 预估金额
        OrderService->>CapitalRecord: 6.2 为所有成员生成差额退款流水
        OrderService->>GroupOrderMember: 6.3 累计退款金额
    else 实际金额 >= 预估金额
        Note over OrderService: 系统不生成差额退款流水
    end

    %% 第七阶段：确认送达
    发起人->>前端页面: 7. 点击"商品已送达"
    前端页面->>OrderController: 7.1 markDelivered(orderId)
    OrderController->>OrderService: 7.2 markDelivered(orderId, currentUser)
    OrderService->>GroupOrder: 7.3 更新状态为 WAIT_RECEIVE
    OrderService->>GroupOrderMember: 7.4 将所有成员收货状态更新为 WAIT_CONFIRM
    OrderService->>NotificationService: 7.5 发送可取货通知
    NotificationService-->>参与者: 通知取货

    %% 第八阶段：参与者确认收货
    参与者->>前端页面: 8. 点击确认收货
    前端页面->>OrderController: 8.1 confirmReceive(orderId)
    OrderController->>OrderService: 8.2 confirmReceive(orderId, currentUser)
    OrderService->>GroupOrderMember: 8.3 更新成员状态为 RECEIVED

    %% 第九阶段：自动确认收货
    OrderScheduler->>OrderService: 9.1 定时检查待确认成员
    OrderService->>GroupOrderMember: 9.2 将超时成员状态更新为 AUTO_RECEIVED
    Note over OrderService,OrderScheduler: 发起人确认送达后，参与者若 30 分钟内未点击确认收货，系统自动确认收货

    %% 第十阶段：完成拼单与结算
    OrderService->>GroupOrder: 10.1 检查是否所有成员均已收货
    OrderService->>GroupOrder: 10.2 更新状态为 COMPLETED
    OrderService->>CapitalRecord: 10.3 生成结算给发起人的流水
    OrderService->>NotificationService: 10.4 发送拼单完成通知
    NotificationService-->>发起人: 拼单完成通知
    NotificationService-->>参与者: 拼单完成通知
```

**图 X-X 拼单主流程协作图**

**图注：**
本图展示拼单主流程中参与者、前端页面、控制类、服务类和核心实体之间的通信关系与职责分工。主要流程包括：发起人创建拼单、参与者加入并支付、系统自动成团、发起人上传凭证、系统执行差额退款、发起人确认送达、参与者确认收货、系统自动确认收货、以及完成拼单与结算等关键环节。

**关键业务规则说明：**
1. 发起人发布拼单后自动加入成员列表
2. 发起人也必须完成支付
3. 参与者加入后需要支付才真正占用名额
4. 只有当人数满员且所有参与者都已支付时，拼单才会进入 GROUPED 状态
5. 成团后系统向所有成员发送通知
6. 拼单成团后，发起人必须上传订单凭证
7. 若实际金额低于预估金额，系统自动退差额
8. 发起人确认送达后，拼单转为 WAIT_RECEIVE
9. 参与者确认收货后更新状态
10. 超时 30 分钟未确认的成员由系统自动确认
11. 所有成员都收货后，拼单进入 COMPLETED
12. 拼单完成后系统生成对发起人的结算流水
